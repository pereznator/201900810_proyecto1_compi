const importacion = require("../asdf/hola");

class Prueba {
    metodo1(param1, param2) {
        //Este es un comentario!
        console.log(param1);
        let nombre = "Jorge";
        const numero = 244.3;
        var boll = false
        metodo2(param1, "valor2", 3, true)
        for (let x = 0; x <= 10; x++) {
            if (x > "mensaje") {
                
            }
        }
        
        while (true + 4 == param2 && "Jorge" != false) {
            
        }
        
        switch (identii) {
            case "hola":
                console.log("asf")
                break;
            default:
                metodo2();
                break;
        }
        
        do{
            
        }while (false == false)
    }
    
    metodo2(valor1, valor2, valor3, valor4) {
        
    }
}


class Clase2 {
    /**
     * 
     * @param {type} libro
     * @param {type} extension
     * @return void
     */
    verLibros(libro, extension) {
        var contenido = "Este es el contenido";
        if (libro >= extension || contenido**3 == false) {
            console.log(libro%2);
        }
    }
    
}
